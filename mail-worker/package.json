{
  "name": "tangled-mail-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that sends contact-form and reference-photo emails with attachments (via Resend) for tangledwithlove.com",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
